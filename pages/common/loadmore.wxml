<template name="loadmore">
  <l-loadmore show="{{loadMoreConfig.show}}" type="{{loadMoreConfig.type}}" color="{{loadMoreConfig.color}}" line="{{loadMoreConfig.line}}" end-text="{{loadMoreConfig.endText}}" loading-text="{{loadMoreConfig.loadingText}}" />
</template>